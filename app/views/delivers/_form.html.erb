<%= form_with(model: deliver, local: true) do |form| %>
  <% if deliver.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(deliver.errors.count, "error") %> prohibited this deliver from being saved:</h2>

      <ul>
      <% deliver.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :id_module %>
    <%= form.number_field :id_module %>
  </div>

  <div class="field">
    <%= form.label :file %>
    <%= form.text_field :file %>
  </div>

  <div class="field">
    <%= form.label :id_team %>
    <%= form.number_field :id_team %>
  </div>

  <div class="field">
    <%= form.label :grade %>
    <%= form.number_field :grade %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
